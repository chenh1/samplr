import React from 'react';

const TrackControl = ({}) => {
    return (
        <div className="">
            <button>Record</button>
            <button>Effects</button>
        </div>
    );
};

export default TrackControl;